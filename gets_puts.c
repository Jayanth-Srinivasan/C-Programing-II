#include <stdio.h>
#include <stdlib.h>

int main()
{
    char str [35];

    printf("Enter your string: ");
    gets(str);
    printf ("The string you entered is :");
    puts(str);

    return 0;
}
